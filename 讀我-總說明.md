# 一億作戰系統 · 總說明

三個部分：① 手機 App（網頁）② 日曆提醒 ③ 後端（選配）。
你 iPhone 只要做 ① 和 ②，就能開始用。③ 是「新聞自動更新」，之後想要再弄。

------------------------------------------------------------
## 資料夾長怎樣
```
一億作戰系統/
├── app/                ← 手機 App（丟 GitHub Pages）
│   ├── index.html      首頁（進去有五個工具）
│   ├── quest.html      每日任務（遊戲）
│   ├── budget.html     信封預算
│   ├── center.html     作戰中心（存錢月曆・繳款・刷卡指南）
│   ├── roadmap.html    一億路線圖
│   ├── plan.html       三個月起跑計畫
│   ├── manifest.json / sw.js / .nojekyll   （PWA 設定，別刪）
│   └── icons/          （App 圖示）
│
├── backend/            ← 後端（選配，之後想要新聞自動更新再用）
│   ├── worker.js
│   ├── wrangler.toml
│   └── README.md
│
├── reminders.ics       ← 日曆提醒（雙擊匯入 iPhone）
└── 讀我-總說明.md       ← 這份
```

------------------------------------------------------------
## ① 手機 App —— 放上 GitHub，裝到 iPhone（約 5 分鐘）

1. 到 github.com 建一個新 repo（Public），例如 `okr`。
2. Add file → Upload files → 把 **app 資料夾「裡面的所有東西」** 拖進去
   （是裡面的檔案，不是整個 app 資料夾；icons 資料夾要一起）。
3. Commit。
4. Settings → Pages → Source 選 `main` 分支、`/ (root)` → Save。
5. 等一分鐘，拿到網址：`https://你的帳號.github.io/okr/`
6. iPhone 用 **Safari** 打開那個網址 → 分享鍵 → **「加入主畫面」** → 命名「一億作戰」。

完成！桌面出現金色圖示，點開就是全螢幕 App，五個工具都在首頁。
> 一定要用 Safari（Chrome 不能加主畫面）。加主畫面與離線只在 https 網址生效，直接開檔案不算。

------------------------------------------------------------
## ② 日曆提醒 —— reminders.ics

把 `reminders.ics` 傳到手機（AirDrop／自己 email 給自己都行），點開 → 「加入全部」。
會有兩個提醒：
- 每月 10 號「先轉 11,000 再花」（每月重複）
- 一次性：9/11 前停掉定期定額

------------------------------------------------------------
## ③ 後端（選配）—— 新聞自動更新

現在 App 的財經簡報是內建快照。想要「每天自動抓台股/幣/新聞＋Claude 判讀」，
照 `backend/README.md` 用 wrangler 部署到 Cloudflare，再把 quest.html 最上面的
`API_BASE` 改成你的 Worker 網址、重新上傳即可。不弄也完全能用。

------------------------------------------------------------
## 進度會存在哪？
存在你手機瀏覽器裡（localStorage），部署到網址後正常保存。換手機不會同步。
所有工具各自獨立記錄，打的勾、存的錢、記的帳都會留著。

## 最重要的一句
工具做再多都是輔助。真正讓進度條動起來的，是每月 10 號那筆「先存 11,000」。
先把這個做出來，其他慢慢用。
